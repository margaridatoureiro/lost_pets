<h1>Checkout these cute pets 🐶😺🐢</h1>

<% @pets.each do |pet| %>
  <p><%= link_to pet.name, pet_path(pet) %></p>
  <p>Species: <%= pet.species %></p>
  <p>Lives in <%= pet.address %></p>
<% end %>

<%= link_to "Add a lost pet", new_pet_path %>